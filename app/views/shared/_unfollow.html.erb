 
<%= form_for get_current_user.relationships.find_by_followed_id(@user),
                         :html => { :method => :delete } ,
                         :remote => false do |f| %>
	<br>
	<div class="actions"> <%= f.submit "Unfollow" %></div>
<% end %>
 
	
 
